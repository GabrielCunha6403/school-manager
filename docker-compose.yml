version: '3.7'

services:
  postgres:
    image: postgres:latest
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=schoolmanager
    volumes:
      - schoolmanager:/postgres

volumes:
  schoolmanager:
